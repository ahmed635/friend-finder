spring:
  application:
    name: friend-finder-service
  banner:
    location: classpath:banners/banner.txt
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=friend-finder;encrypt=true;trustServerCertificate=true
    username: maher
    password: 123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
